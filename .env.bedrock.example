# Configuration WordPress Bedrock
# Copiez ce fichier dans le dossier wp_debrock/ en tant que .env
# et ajustez les valeurs selon vos besoins

####> PROJECT INFO ###
# Ces valeurs doivent correspondre à celles configurées dans .env pour docker-compose
NAME=wordpress-debrock
BASE=mariadb
FOLDER_BACK=wp_debrock
###< PROJECT INFO ###

###> DATABASE SETTINGS ###
# Ces valeurs doivent correspondre à celles configurées dans .env pour docker-compose
DB_NAME=${NAME}_${BASE}
DB_USER='user'
DB_PASSWORD='password'
DB_HOST='127.0.0.1'
DB_PORT=3306

# Optionally, you can use a data source name (DSN)
# When using a DSN, you can remove the DB_NAME, DB_USER, DB_PASSWORD, and DB_HOST variables
# DATABASE_URL="mysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# Optional database variables
DB_PREFIX='wp_'
###< DATABASE SETTINGS ###

###> ENVIRONMENT ###
WP_ENV='development'

# development, staging, production
# - development: active WP_DEBUG
# - staging: désactive WP_DEBUG, active WP_DEBUG_DISPLAY
# - production: désactive tous les modes debug
###< ENVIRONMENT ###

###> URLS ###
WP_HOME='http://localhost:8000'
WP_SITEURL="${WP_HOME}/${FOLDER_BACK}"

# Alternative pour HTTPS:
# WP_HOME='https://votredomaine.com'
###< URLS ###

###> DEBUG SETTINGS ###
# Chemin absolu vers le fichier de log (optionnel)
# WP_DEBUG_LOG="${FOLDER_BACK}/debug.log"

# Afficher les erreurs (true seulement en development)
# WP_DEBUG_DISPLAY=true
###< DEBUG SETTINGS ###

###> WORDPRESS SECURITY KEYS ###
# Générez vos propres clés sur: https://generator.corbisier.fr
# IMPORTANT: Remplacez ces valeurs par des clés uniques générées !

AUTH_KEY='generateme'
SECURE_AUTH_KEY='generateme'
LOGGED_IN_KEY='generateme'
NONCE_KEY='generateme'
AUTH_SALT='generateme'
SECURE_AUTH_SALT='generateme'
LOGGED_IN_SALT='generateme'
NONCE_SALT='generateme'
###< WORDPRESS SECURITY KEYS ###

###> OPTIONAL SETTINGS ###
# Désactiver l'éditeur de fichiers dans l'admin WordPress
# DISALLOW_FILE_EDIT=true

# Désactiver les mises à jour automatiques
# AUTOMATIC_UPDATER_DISABLED=true

# Limiter les révisions de posts
# WP_POST_REVISIONS=3

# Activer le cache
# WP_CACHE=true
###< OPTIONAL SETTINGS ###
